{
  "name": "software-testing-assignment",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "jest --coverage",
    "coveralls": "cat coverage/lcov.info | coveralls"
  },
  "devDependencies": {
    "jest": "^29.6.1",
    "coveralls": "^3.2.0"
  },
  "jest": {
    "testEnvironment": "node",
    "collectCoverageFrom": [
      "src/**/*.js",
      "!src/.internal/**"
    ]
  }
}
